#ifndef MAIN_H
#define MAIN_H

#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <unistd.h>

/* prototypes des fonctions du projet file_io */
ssize_t read_textfile(const char *filename, size_t letters);
int create_file(const char *filename, char *text_content);
int append_text_to_file(const char *filename, char *text_content);

/* exigé par le sujet, même si tu ne l’utilises pas ici */
int _putchar(char c);

#endif /* MAIN_H */
